(set-logic NRA)
(synth-fun a_o ( ( delta_i Real ) ) Real )
(declare-var delta Real)
( constraint (=> (and (<= 0.0 delta)) (and (<= 0.0 delta)      (<= (- (* 4.0 (a_o delta) (a_o delta) ) 2.0) delta)      (<= (- 2.0 (* 4.0 (a_o delta) (a_o delta) )) delta))))
( check-synth )
(exit)
